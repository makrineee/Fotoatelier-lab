# Secrets — never commit
.env
.env.local
**/.env
**/.env.local

# System files
.DS_Store

# Dependencies & logs
node_modules/
*.log

# Local-only — never push (internal config & holding notes)
.claude/
_holding/
